# slimtest

Backend boilerplate / project template for PHP 8.3 based on Slim Framework.

Проект используется как заготовка для новых backend-приложений, чтобы не настраивать инфраструктуру и базовые инструменты с нуля при каждом новом проекте.

---

## 🎯 Назначение проекта

`slimtest` — это стартовый шаблон для backend-разработки на PHP, предназначенный для:

- быстрого старта новых проектов;
- единообразной структуры приложений;
- готовой инфраструктуры для разработки и тестирования;
- фокуса на бизнес-логике, а не на настройке окружения.

Проект используется как основа для реальных production-сервисов.

---

## 🧱 Стек технологий

- **PHP 8.3**
- **Slim Framework 4**
- **PSR-7 / PSR-15**
- **PHP-DI** — Dependency Injection
- **Doctrine ORM**
- **Guzzle** — HTTP client
- **Symfony Console**
- **dotenv**

### Качество кода и разработка
- **PHPUnit** — unit-тесты
- **phplint** — статическая проверка синтаксиса
- **Psalm** (поддерживается, используется при необходимости)
- **Makefile** — упрощение команд
- **Docker / docker-compose** — окружение разработки

---

## 📁 Структура проекта

```text
.
├── bin/            # CLI команды
├── config/         # Конфигурация приложения
├── public/         # HTTP entry point
├── src/            # Основной код приложения
├── tests/          # Unit-тесты
├── docker/         # Docker конфигурация
├── Makefile
├── composer.json
└── phpunit.xml
